# Force LF on all text so the snapshot is byte-identical on Windows and Linux CI.
* text=auto eol=lf
*.swift text eol=lf
*.json  text eol=lf
*.md    text eol=lf
*.ps1   text eol=lf
*.sh    text eol=lf

Annotated accessibility overlays used as inline evidence in proxy PRs.

Assets only - no code. Kept on an orphan branch so PR review does not depend on the
GitHub Pages gallery being deployed.

`*-before-scannerfix.png` come from run 32174663150 (scanner querying 7 element types).
`*-after-scannerfix.png`  come from run 32186444117 (scanner querying 11).
Both are the same cards on the same SDK build; only the scanner differs.
